# Test basic configuration

pid_path: test.jackal.pid

debug:
  port: 16060

logger:
  level: debug
  log_path: test.jackal.log

storage:
  type: memory

c2s:
  - id: default
    transport:
      port: 15222

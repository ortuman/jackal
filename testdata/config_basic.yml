# Test basic configuration

pid_path: test.jackal.pid

debug:
  port: 6060

logger:
  level: debug
  log_path: test.jackal.log

storage:
  type: memory

c2s:
  - id: default
    transport:
      port: 5222
